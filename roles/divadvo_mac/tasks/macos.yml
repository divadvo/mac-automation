## macOS system defaults configuration
## Based on https://github.com/geerlingguy/dotfiles/blob/master/.osx

- name: Apply macOS system defaults
  import_tasks: macos_defaults.yml
  tags:
    - macos
    - system
    - never

- name: Apply optional macOS system defaults
  import_tasks: macos_defaults_optional.yml
  tags:
    - macos-optional
    - never
  # Additional settings from macos-defaults.com
  # Run with: uv run ./playbook.yml --tags macos-optional --ask-become-pass
