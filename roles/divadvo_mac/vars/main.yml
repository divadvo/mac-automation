# Command line tools to install via Homebrew
homebrew_packages:
  # Version Control
  - git
  - git-lfs
  - git-filter-repo
  - gh
  
  # Development Tools
  - ansible
  - anomalyco/tap/opencode
  - mise
  - neovim
  - uv
  - ruby-build
  - just
  - xcodegen

  # Shells
  - bash
  - fish
  
  # Network Tools
  - wget
  - curl
  - flyctl
  - hcloud
  - miniserve
  - render
  - xh
  
  # Media Tools
  - duti
  - ffmpeg
  - yt-dlp
  
  # Search & File Tools
  - fd
  - ripgrep
  - rsync
  
  # Databases
  - redis
  - postgresql@{{ postgresql_version }}
  
  # System Utilities
  - coreutils
  - dockutil
  
  # Security Tools
  - b2-tools
  - bitwarden-cli
  
  # Optional Tools (can be commented out for minimal install)
  # Fuzzy Finding & Navigation
  - fzf
  - zoxide
  
  # # Terminal Multiplexers
  - tmux
  - zellij
  
  # # Help & Documentation
  - tlrc
  - cheat
  
  # # File Viewing & Management
  - lsd
  - tree
  - bat
  # - micro
  
  # # Development Tools
  - git-delta
  - gum
  - jq
  - typst
  
  # # System Monitoring
  - htop
  # - lnav
  - bottom

  # # System and Network Diagnostics
  - nmap
  - qpdf
  - rclone

# GUI applications to install via Homebrew Cask
homebrew_cask_packages:
  # Development Tools
  - visual-studio-code
  - jetbrains-toolbox
  - iterm2
  - ngrok
  - t3-code
  
  # Web Browsers
  - google-chrome
  - brave-browser
  - firefox
  
  # Communication
  - whatsapp
  - discord
  - zoom
  - teamviewer
  
  # Media & Entertainment
  - iina
  - spotify
  - musescore
  
  # Productivity & Office
  - microsoft-office
  - cleanshot
  
  # System Utilities
  - raycast
  - rectangle
  - orbstack
  - betterdisplay
  - unnaturalscrollwheels
  - tailscale-app
  
  # Security
  - 1password
  - bitwarden
  
  # Creative & Design
  - bambu-studio
  - adobe-creative-cloud
  
  # Enterprise/Business
  - citrix-workspace
  
  # AI Tools
  - chatgpt
  - claude
  - claude-code@latest
  - codex
  - macwhisper
  
  # Fonts
  #- font-meslo-lg-nerd-font

# Minimal GUI applications for testing
homebrew_cask_packages_test:
  - visual-studio-code
  - iterm2
  #- google-chrome
  #- raycast
  #- rectangle

# Runtime version configuration
node_version: "24"
ruby_version: "4"
bun_version: "1"
rust_version: "1"
python_versions:
  - "3.13"
  - "3.14"
postgresql_version: "18"

# Python tools to install via uv tool install
uv_tools:
- name: build
- name: ruff

# Oh My Zsh custom plugins
ohmyzsh_custom_plugins:
  - { name: zsh-autosuggestions, repo: "https://github.com/zsh-users/zsh-autosuggestions.git" }
  - { name: zsh-you-should-use, repo: "https://github.com/MichaelAquilina/zsh-you-should-use.git" }
  - { name: pnpm, repo: "https://github.com/ntnyq/omz-plugin-pnpm.git" }

# Claude Code extensions — installed user-wide (available in every repo).
# See roles/divadvo_mac/tasks/claude.yml. Keep in sync with ubuntu-setup.sh.
#
# MCP servers. HTTP servers set `url:`; stdio servers set `command:` (argv list).
claude_mcp_servers:
  - { name: context7, transport: http, url: "https://mcp.context7.com/mcp" }
  - { name: turbostarter, transport: http, url: "https://www.turbostarter.dev/mcp" }
  - { name: playwright, transport: stdio, command: ["npx", "-y", "@playwright/mcp@latest"] }
  - { name: chrome-devtools, transport: stdio, command: ["npx", "-y", "chrome-devtools-mcp@latest"] }
  - { name: shadcn, transport: stdio, command: ["npx", "-y", "shadcn@latest", "mcp"] }

# Plugins installed from marketplaces. `source` is the marketplace to register
# (github repo / url); `marketplace` is its declared name for the install id.
claude_plugins:
  - { name: cloudflare, marketplace: cloudflare, source: "cloudflare/skills" }
  - { name: caveman, marketplace: caveman, source: "juliusbrussee/caveman" }
  - { name: ponytail, marketplace: ponytail, source: "DietrichGebert/ponytail" }

# Global npm CLIs related to the Claude workflow (installed via mise's node/npm).
claude_global_npm_tools:
  - "@fission-ai/openspec@latest"

# Codex plugins — see roles/divadvo_mac/tasks/codex.yml. Codex mirrors Claude's
# marketplace commands, so the shape here matches claude_plugins above. Only
# packages with a native Codex plugin build belong here; packages that ship
# skills but no Codex hooks go in cross_agent_skills instead.
codex_plugins:
  - { name: ponytail, marketplace: ponytail, source: "DietrichGebert/ponytail" }

# Own skills, living in skills/ in this repo. Symlinked rather than copied, so
# editing the SKILL.md here takes effect immediately with no reinstall step.
agent_skills:
  - houserules

# Third-party skills for the agents that have no plugin build of them. Claude
# Code already gets these through claude_plugins, so it is deliberately absent.
# `probe` is a skill directory the source installs — used to skip re-running.
cross_agent_skills:
  - { source: "cloudflare/skills", probe: wrangler, agents: [codex, opencode] }
  - { source: "JuliusBrussee/caveman", probe: caveman, agents: [codex] }

# User Configuration - Override these in local.yml (gitignored)
# The bootstrap script creates local.yml automatically
user_email: "your@email.com"
user_name: "Your Name"

# Directory structure for projects and repositories
projects_dir: "~/pr"
github_dir: "~/pr/github"
github_other_dir: "~/pr/github-other"
sandbox_dir: "~/pr/sandbox"

# Repository configuration
# Repositories that are always cloned to priority directory (format: "owner/repo")
priority_repos:
  - "divadvo/divadvo-scripts"
  # Add your important repositories here

# Clone all personal repositories pushed after this date
repos_pushed_after: "2022-01-01"

# Set to true to install only essential casks for faster testing
testing_mode: false
